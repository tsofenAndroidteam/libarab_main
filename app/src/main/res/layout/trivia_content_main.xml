<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:ads="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="Trivia.StartQuiz"
    tools:showIn="@layout/trivia_app_bar_main">


    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:padding="10dp"
        android:background="@drawable/trivia_box_shape"
        android:id="@+id/relativeLayout"
        android:textAlignment="center">

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:id="@+id/title"
            android:text="السلام في الشرق الاوسط"
            android:textSize="18sp"
            android:textStyle="normal"
            android:textColor="#000000"
            android:layout_alignParentEnd="false" />

    </RelativeLayout>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start quiz"
        android:textColor="#ffffff"
        android:paddingLeft="6dp"
        android:paddingRight="6dp"
        android:id="@+id/btnStart"
        android:background="@drawable/trivia_btny"
        android:drawableTop="@drawable/fav_ic_menu_add"
        android:layout_below="@+id/relativeLayout"
        android:layout_alignStart="@+id/button4"
        android:layout_marginTop="39dp"
        android:layout_alignEnd="@+id/button4" />

    <Button
        android:text="Book View"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="#ffffff"
        android:paddingLeft="6dp"
        android:paddingRight="6dp"
        android:background="@drawable/trivia_btny"
        android:id="@+id/button4"
        android:drawableTop="@drawable/book_info"
        android:scaleY="0.6"
        android:layout_marginTop="49dp"
        android:scaleWidth="10dp"
        android:layout_below="@+id/btnStart"
        android:layout_centerHorizontal="true" />


</RelativeLayout>
